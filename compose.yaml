services:
  workflow-auditor:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      DEFAULT_HOURLY_RATE: "35"
    restart: unless-stopped
